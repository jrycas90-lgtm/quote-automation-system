services:
  postgres:
    image: postgres:16
    container_name: quote_automation_db
    environment:
      POSTGRES_DB: quote_automation
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - quote_pgdata:/var/lib/postgresql/data

volumes:
  quote_pgdata:
